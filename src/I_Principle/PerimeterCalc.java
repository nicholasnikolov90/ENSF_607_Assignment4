package I_Principle;

//PerimeterCalc is a method which calculates the perimeter of a shape.


public class PerimeterCalc {
	
	//calculates and returns the perimeter of the shape
	double perimeter (Shape2D shape) {
		return shape.calculatePerimeter();
	}
}
